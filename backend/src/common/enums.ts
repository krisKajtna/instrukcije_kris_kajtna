export enum Role {
    STUDENT = 'STUDENT',
    TUTOR = 'TUTOR',
    ADMIN = 'ADMIN',
}

export enum TransactionType {
    DEPOSIT = 'DEPOSIT',
    PAYMENT = 'PAYMENT',
    REFUND = 'REFUND',
    INCOME = 'INCOME',
}

export enum ReservationStatus {
    PENDING = 'PENDING',
    CONFIRMED = 'CONFIRMED',
    COMPLETED = 'COMPLETED',
    CANCELLED = 'CANCELLED',
}
